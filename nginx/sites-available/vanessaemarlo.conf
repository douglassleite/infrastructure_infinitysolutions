# ===========================================
# Vanessa e Marlo - vanessaemarlo.com.br
# Site de casamento (container separado)
# ===========================================

# Redirect www to non-www
server {
    listen 443 ssl;
    http2 on;
    server_name www.vanessaemarlo.com.br;

    ssl_certificate /etc/letsencrypt/live/vanessaemarlo.com.br/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/vanessaemarlo.com.br/privkey.pem;

    return 301 https://vanessaemarlo.com.br$request_uri;
}

server {
    listen 443 ssl;
    http2 on;
    server_name vanessaemarlo.com.br;

    # SSL Configuration
    ssl_certificate /etc/letsencrypt/live/vanessaemarlo.com.br/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/vanessaemarlo.com.br/privkey.pem;

    # SSL Settings
    ssl_session_timeout 1d;
    ssl_session_cache shared:SSL:50m;
    ssl_session_tickets off;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;

    # HSTS
    add_header Strict-Transport-Security "max-age=63072000" always;

    # Wedding Site - Vanessa e Marlo (container separado)
    location / {
        limit_req zone=web burst=100 nodelay;

        proxy_pass http://vanessaemarlo;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 60s;
    }

    # Cache para arquivos estáticos
    location ~* \.(css|js|jpg|jpeg|png|gif|ico|svg|woff|woff2|ttf|eot|html)$ {
        proxy_pass http://vanessaemarlo;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_valid 200 30d;
        add_header Cache-Control "public, immutable, max-age=2592000";
    }

    # Health check
    location /health {
        proxy_pass http://vanessaemarlo/health;
        access_log off;
    }
}
